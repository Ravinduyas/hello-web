# Repo-wide ignores. Each sub-project (hello-web, hello-manage) also has its own.
node_modules/
dist/
build/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Client-supplied fleet photos — source material, not repo content.
command/fleet/
